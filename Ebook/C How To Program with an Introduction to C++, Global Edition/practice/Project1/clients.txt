#include <stdio.h>
#include <stdlib.h>

struct linkedList
{
	char value;
	linkedList* nextPtr;
};
struct queue
{
	char value;
	linkedList* nextPtr;
};
struct stack
{
	char value;
	linkedList* nextPtr;
};

typedef struct linkedList LINKEDLIST;
typedef LINKEDLIST* llPtr;
typedef struct stack STACK;
typedef STACK* stackPtr;
typedef struct queue QUEUE;
typedef QUEUE* queuePtr;

void insertLinkedList(llPtr*, char);
void deleteLinkedList(llPtr*, char);
void insertQueue(queuePtr);
void removeQueue(queuePtr);
void insertStack(stackPtr);
void popStack(stackPtr);

void printLinkedList(llPtr*);
void printQueue(queuePtr*);
void printStack(stackPtr*);


int main(void)
{
	puts("-----------LinkedList----------\n");
	llPtr startPtr;
	puts("-----------LinkedList----------\n");
}

void insertLinkedList(llPtr* ptr, char value)
{
	struct linkedList* a = malloc(sizeof(int));
	llPtr newPtr = malloc(sizeof(a));
}
void deleteLinkedList(llPtr*ptr, char value)
{

}


void insertQueue(queuePtr *ptr)
{
	
}
void removeQueue(queuePtr *ptr)
{

}

void insertStack(stackPtr *ptr)
{

}
void popStack(stackPtr *ptr)
{
	
}

void printLinkedList(llPtr* ptr)
{
	
}
void printQueue(queuePtr* ptr)
{

}
void printStack(stackPtr* ptr)
{

}

